{
  "name": "screenplay-visualizer",
  "version": "1.0.0",
  "description": "MCP Server + Electron Visualizer for Screenplay ensemble coding",
  "type": "module",
  "main": "dist/bootstrap.js",
  "scripts": {
    "build": "tsc && tsc -p tsconfig.preload.json",
    "start": "npm run build && electron .",
    "dev": "npm run build && electron .",
    "clean": "rm -rf dist",
    "test:e2e": "npm run build && node dist/tests/run-all-scenarios.js"
  },
  "keywords": ["mcp", "electron", "screenplay", "visualization"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "electron": "^28.0.0",
    "fastmcp": "^3.0.0",
    "zod": "^3.22.4",
    "d3": "^7.8.5",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/d3": "^7.4.3",
    "@types/uuid": "^9.0.7",
    "typescript": "^5.3.3"
  }
}
